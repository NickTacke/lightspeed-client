---
"lightspeed-client": minor
---

add product sub-resources relations/filterValues/attributes (client.products.relations(id) etc.) and a top-level quantity-discounts resource (client.quantityDiscounts). product attributes are read/update/delete only (the API has no create). all live-validated.
