pre-commit:
  commands:
    biome:
      glob: "*.{ts,json}"
      run: bunx biome check --write {staged_files} && git add {staged_files}
commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
